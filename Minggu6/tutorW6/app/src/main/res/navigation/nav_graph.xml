<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <!-- Fragment Bottom Nav -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.tutorw6.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">

        <!-- Tambahan Action untuk Safe Args -->
        <action
            android:id="@+id/action_homeFragment_to_detailFragment"
            app:destination="@id/detailFragment" />
        <action
            android:id="@+id/action_homeFragment_to_formFragment"
            app:destination="@id/formFragment" />
    </fragment>

    <fragment
        android:id="@+id/cartFragment"
        android:name="com.example.tutorw6.CartFragment"
        android:label="Cart"
        tools:layout="@layout/fragment_cart" />

    <!-- Fragment Detail & Form -->
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.example.tutorw6.DetailFragment"
        android:label="Detail"
        tools:layout="@layout/fragment_detail">
        <argument
            android:name="BREAD_ID"
            app:argType="integer"
            android:defaultValue="-1" />
    </fragment>

    <fragment
        android:id="@+id/formFragment"
        android:name="com.example.tutorw6.FormFragment"
        android:label="Form"
        tools:layout="@layout/fragment_form">
        <argument
            android:name="BREAD_ID"
            app:argType="integer"
            android:defaultValue="-1" />
    </fragment>

</navigation>